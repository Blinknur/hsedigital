apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hse-ingress
spec:
  tls:
    - hosts:
        - staging.hse.digital
      secretName: hse-staging-tls-cert
  rules:
    - host: staging.hse.digital
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hse-app-service
                port:
                  number: 80
